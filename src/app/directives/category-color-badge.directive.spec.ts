import { CategoryColorBadge } from './category-color-badge';

describe('CategoryColorBadge', () => {
  it('should create an instance', () => {
    const directive = new CategoryColorBadge();
    expect(directive).toBeTruthy();
  });
});
